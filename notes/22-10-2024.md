## Description

I continued to implement the dsa in c, trying to make everything with no loops only recursion is valid.

Done => DynamicArray.

I learn a new function in c called realloc -> it takes a ptr that points to a chunk of memory and it increase its size.
